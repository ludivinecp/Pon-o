<nav class="navbar navbar-fixed-top navbar-default " role="navigaton" style="/* IE10+ */
background-image: -ms-linear-gradient(top, #FA9C06 10%, #D98805 90%);

/* Mozilla Firefox */
background-image: -moz-linear-gradient(top, #FA9C06 10%, #D98805 90%);

/* Opera */
background-image: -o-linear-gradient(top, #FA9C06 10%, #D98805 90%);

/* Webkit (Safari/Chrome 10) */
background-image: -webkit-gradient(linear, left top, left bottom, color-stop(10, #FA9C06), color-stop(90, #D98805));

/* Webkit (Chrome 11+) */
background-image: -webkit-linear-gradient(top, #FA9C06 10%, #D98805 90%); box-shadow: 0px 1px 2px #888888;
">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header" style="margin-top:25px;">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!-- <%= link_to " Poneo", root_path, class:"navbar-brand"%> -->
      <%= link_to "pONéO !", root_path, {:style=>"color: white; font-family: 'Luckiest Guy', cursive; font-size: 3em; padding-left: 30px;", :class => "css_class"}%>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
      
        <li><%= link_to "Accueil", root_path %></li>
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <%= link_to "Admin", rails_admin_path%>
          <%end%>
          <li style="color:white; padding-top: 16px; padding-left: 30px;">Bonjour <strong><%= current_user.centre.try(:name)%></strong>
          <strong><%= current_user.rider.try(:first_name)%>!</strong></li>
          <li><%= link_to "Mon compte", edit_user_registration_path %></li>
          <li><%= link_to "Mon profil", rider_path(current_user.id)  %></li>
          <li><%= link_to "Me déconnecter", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Connexion", new_user_session_path %></li>
        <% end %>
       

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
</nav>
